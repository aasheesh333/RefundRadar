<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ data extraction rules. allowBackup=false in the manifest is
     the primary guard; these rules reinforce it for cloud-backup & device
     transfer so bank-transaction-derived data never leaves the device. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
